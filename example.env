# PostgreSQL connection used by the backend and the Compose db service.
# DATABASE_URL is optional: when omitted, the backend assembles it from
# POSTGRES_USER/PASSWORD/DB plus the optional POSTGRES_HOST/PORT below.
POSTGRES_USER=archive
POSTGRES_PASSWORD=change-this-database-password
POSTGRES_DB=archive
# POSTGRES_HOST=db
# POSTGRES_PORT=5432

# Optional escape hatch for an externally managed PostgreSQL instance.
# DATABASE_URL=postgresql+psycopg://user:password@db.example.com:5432/archive

# Normal application configuration is completed from the first-run setup page
# and Settings. This includes the administrator, OIDC, SMTP, provider API
# credentials, upload limits, and secure-cookie setting.
#
# The application generates and persists its Fernet encryption key automatically
# under APP_DATA_DIR/config/fernet.key. Do not generate SECRET_KEY manually.
# Existing installations may keep SECRET_KEY temporarily as a one-time
# backwards-compatible bootstrap value; it is copied into persistent storage.
# Required: valid Fernet key used for encryption at rest.
# Generate with:
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
#SECRET_KEY=replace-with-a-generated-fernet-key
#AUTH_COOKIE_SECURE=false


# Optional first-admin bootstrap. If all three are set, the server creates this
# administrator automatically. If they are omitted, /setup remains available.
# PRIMARY_USER_USERNAME=admin
# PRIMARY_USER_EMAIL=admin@example.com
# PRIMARY_USER_PASSWORD=Change-this-during-setup
# Set BYPASS_SETUP=true to require environment bootstrap credentials and skip the web setup menu.
# BYPASS_SETUP=false

# Optional password-protected deployment settings bootstrap.
# Place the encrypted Settings -> Backup JSON at /data/application.json (or set
# APPLICATION_JSON_PATH) and provide its export password. It is only imported
# automatically when the database has no users.
# APPLICATION_JSON_PASSWORD=your-settings-export-password
# APPLICATION_JSON_PATH=/data/application.json


# Browser-facing URL. The frontend proxies /api to the backend in Docker Compose.
#VITE_API_BASE_URL=http://localhost:5173


# New installations should configure provider API keys and OIDC from
# Settings -> OIDC / SSO. The variables below remain supported as
# backwards-compatible fallbacks for existing deployments.
# OIDC_ISSUER_URL=https://login.example.com/realms/archive
# OIDC_CLIENT_ID=archive
# OIDC_CLIENT_SECRET=replace-me
# OIDC_REDIRECT_URI=http://localhost:5173/api/auth/oidc/callback
# OIDC_SCOPES=openid profile email
# OIDC_USER_MATCH_FIELD=email
