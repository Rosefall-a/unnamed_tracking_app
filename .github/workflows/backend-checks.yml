name: Backend checks

on:
  push:
    paths:
      - "src/backend/**"
      - ".github/workflows/backend-checks.yml"
  pull_request:
    paths:
      - "src/backend/**"
      - ".github/workflows/backend-checks.yml"

jobs:
  backend-checks:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: src/backend

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: src/backend/requirements.txt

      - name: Install backend dependencies
        run: python -m pip install -r requirements.txt

      - name: Validate Alembic migration graph
        run: |
          python - <<'PY'
          from alembic.config import Config
          from alembic.script import ScriptDirectory

          config = Config("alembic.ini")
          # alembic.ini uses the container path /app/src/database/migrations.
          # Override it for CI, where the working directory is src/backend.
          config.set_main_option("script_location", "src/database/migrations")
          script = ScriptDirectory.from_config(config)
          heads = script.get_heads()
          print("Alembic heads:", heads)
          if len(heads) != 1:
              raise SystemExit(f"Expected exactly one Alembic head, found {len(heads)}: {heads}")
          PY

      - name: Run mypy
        run: mypy --config-file pyproject.toml src

      - name: Run pylint
        run: |
          pylint src | tee pylint-output.txt
          score=$(grep -oP 'Your code has been rated at \K[0-9.]+(?=/10)' pylint-output.txt)
          echo "Pylint score: $score"
          python -c "import sys; sys.exit(0 if float('$score') >= 9.0 else 1)"
