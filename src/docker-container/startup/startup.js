const phaseNames={INITIALIZING:"Initialising",WAITING_FOR_DATABASE:"Waiting for database",DATABASE_READY:"Database ready",MIGRATING_DATABASE:"Migrating database",STARTING_BACKEND:"Starting backend",STARTING_FRONTEND:"Starting frontend",READY:"Ready",CONFIGURATION_FAILED:"Configuration error",DATABASE_FAILED:"Database failed",MIGRATION_FAILED:"Migration failed",BACKEND_FAILED:"Backend failed",BACKEND_TIMEOUT:"Backend timeout",FRONTEND_FAILED:"Frontend failed",BACKEND_CRASHED:"Backend stopped"};
const labels=["database","migrations","backend","frontend"];
const pretty=v=>v?v.charAt(0).toUpperCase()+v.slice(1):"waiting";
function render(s){document.title=s.overall==="ready"?"Unnamed Tracking":"Unnamed Tracking — Starting";document.querySelector("#phase").textContent=phaseNames[s.phase]||s.phase;document.querySelector("#message").textContent=s.message||"";document.querySelector("#steps").innerHTML=labels.map(k=>`<div class="step ${s[k]||"waiting"}"><span>${k}</span><span>${pretty(s[k])}</span></div>`).join("");if(s.overall==="failed")document.querySelector(".spinner").style.animationPlayState="paused"}
async function poll(){try{const r=await fetch("/_startup/status.json?ts="+Date.now(),{cache:"no-store"});if(r.ok)render(await r.json());const d=await fetch("/_startup/details.txt?ts="+Date.now(),{cache:"no-store"});if(d.ok)document.querySelector("#details").textContent=await d.text()}catch{document.querySelector("#message").textContent="Waiting for the startup service to respond…"}}
poll();setInterval(poll,1000);
