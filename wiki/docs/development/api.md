# API

The Unnamed Tracking App automatically generates interactive API documentation.

To view the API documentation for your instance, open:

`https://your-url/api/docs`

Replace `your-url` with the URL of your Unnamed Tracking App instance.

The API documentation is generated directly from the running application, so it reflects the API available on that instance.
